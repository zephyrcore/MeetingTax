[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "meetingtax"
version = "0.1.0"
description = "Measure the attention cost of meetings from iCalendar exports."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Zephyr" }]
